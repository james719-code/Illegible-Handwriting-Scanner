# Optional Metadata

Paired OCR training does not require CSV annotations. This folder is optional and can be used for:

- train/validation/test split files
- writer metadata
- notes about hard samples
- experiment manifests if you later decide to add them
