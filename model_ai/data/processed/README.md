# Processed Dataset

Use this folder for outputs from the preprocessing pipeline such as:

- contrast-enhanced images
- binarized images
- denoised variants used for model training

If you preprocess paired OCR samples, keep the image stem unchanged so it still maps cleanly to its transcription text.
